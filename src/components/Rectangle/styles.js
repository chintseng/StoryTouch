import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    // width: 100,
    // height: 50,
    borderWidth: 2,
    borderColor: 'red',
  },
});
export default styles;
